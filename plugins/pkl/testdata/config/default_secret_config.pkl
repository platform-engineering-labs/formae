/*
 * © 2025 Platform Engineering Labs Inc.
 *
 * SPDX-License-Identifier: FSL-1.1-ALv2
 */

// Config that uses the default secret (doesn't override it)
amends "formae:/Config.pkl"

agent {
    server {
        nodename = "formae-test"
        hostname = "test.example.com"
        port = 5678
        // Intentionally NOT setting secret to test the default
    }
}
