╔══════════════════════════════════════════════════════════════════════════════╗
║                    STATUS / WATCH — TUI MOCKUPS                             ║
║                                                                              ║
║  This is the core shared component used by:                                  ║
║  - formae status command [--watch]                                           ║
║  - formae apply (after submission, with --watch)                             ║
║  - formae destroy (after submission, with --watch)                           ║
║  - formae cancel (watching cancellation)                                     ║
╚══════════════════════════════════════════════════════════════════════════════╝


================================================================================
LIVE UPDATE BEHAVIOR
================================================================================

  The status/watch view continuously polls the formae agent for command state.
  It does NOT snapshot commands at CLI start — it is a live, dynamic view:

  - New commands that appear on the agent are automatically sorted into the
    view according to the current sort order (like htop with new processes)
  - Commands that complete transition to done/failed state in-place
  - The view is constrained by the --query flag of the CLI status command.
    New commands appearing on the agent are also filtered by the same query,
    so only matching commands enter the view
  - Polling interval is tick-based (replaces the old ClearScreen + sleep loop)
  - The "↻ live" indicator in the header shows active polling


================================================================================
QUERY BAR
================================================================================

  The query bar is always visible as a line above the footer, between the
  command list and the keybinding hints:

  │  ...commands...                                                              │
  ├──────────────────────────────────────────────────────────────────────────────┤
  │  ● state:InProgress                                        /: edit query    │
  ├──────────────────────────────────────────────────────────────────────────────┤
  │  ↑↓: select  enter: details  →←: column  s: toggle sort  q: quit  ?: help  │
  └──────────────────────────────────────────────────────────────────────────────┘

  When no query is active:

  │  /: query                                                                    │

  When editing (after pressing /):

  │  / state:InProgress█                              enter: apply  esc: cancel  │

  Behavior:
  - On start: populated from --query flag value, or empty (show all)
  - ● indicator (orange) shows when a query is active
  - Press / to focus — cursor appears, you edit the query in-place
  - Same query syntax as --query on the CLI (formae query language:
    state:InProgress, type:apply, id:cmd-abc123, etc.)
  - Press enter to apply — the view re-fetches from the agent with the new
    query. Commands not matching disappear, new matching commands appear
  - Press esc to cancel edit — reverts to the previous query
  - The query is server-side: the agent filters commands before sending,
    so only matching commands are polled and displayed
  - Empty query = show all commands (same as no --query flag)
  - The query bar also constrains live updates: new commands from the agent
    only appear if they match the current query


================================================================================
VIEW 1: Multi-command summary (default when viewing multiple commands)
================================================================================

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  formae status command                                             ↻ live   │
  ├──────────────────────────────────────────────────────────────────────────────┤
  │                                                                              │
  │       ID            Command  Mode       Progress              ✓  ✗  ◐  ○  Time  Age │
  │  ──────────────────────────────────────────────────────────────────────────────── │
  │  ⣟   cmd-jkl012    apply    reconcile  █████████▒▒░░⋅⋅⋅⋅⋅ 7/12  5  2  2  3  01:58  ~2m │
  │  ⣟   cmd-abc123    apply    reconcile  █████████████░░⋅⋅⋅ 12/15 10  0  2  3  00:42  42s │
  │  ✗   cmd-ghi789    apply    patch      completed 3/3          1  1  0  1  00:33  10m │
  │  ✓   cmd-def456    destroy             completed 8/8          8  0  0  0  00:15   3h │
  │  ✓   cmd-xyz999    apply    reconcile  completed 15/15       15  0  0  0  00:08  >1mo│
  │  ...                                                                (scrollable) │
  ├──────────────────────────────────────────────────────────────────────────────────┤
  │  /: query                                                                        │
  ├──────────────────────────────────────────────────────────────────────────────────┤
  │  ↑↓: select  enter: details  →←: column  s: toggle sort  q: quit    ?: help     │
  └──────────────────────────────────────────────────────────────────────────────────┘

  Notes:

  Columns:
  - Status symbol on far left — spinner for in-progress, ✗ for failed, ✓ for done
    In-progress + failing: red spinner. Finished failed: dimmed red ✗
  - ID: command ksuid
  - Command: apply/destroy
  - Mode: reconcile/patch
  - Progress: segmented bar for in-progress, "completed N/N" text for terminal
  - ✓ ✗ ◐ ○: per-status count columns (done, failed, in-progress, pending)
  - Time: duration — how long the command ran (MM:SS format)
  - Age: how old the command is — compact relative time (max 4 chars)
    Ranges: Ns, Nm, Nh, Nd, >Nmo (1-9 months), >Ny (10mo+)
    Display is approximate for older ages, but sorting uses the real
    timestamp for accurate ordering

  Progress bar:
  - Segmented by status with distinct characters AND colors:
    █ (green) = done/skipped, ▒ (red) = failed, ░ (light gray) = in-progress,
    ⋅ (dark gray) = pending
  - Characters provide density hierarchy (solid → hatched → light → dots)
    so the bar is readable without color — colorblind-safe by default
  - Bar is elastic — fills remaining space after fixed columns
  - Completed commands show "completed N/N" text instead of bar (dimmed)

  Row coloring:
  - Running + failing: entire row bright red, red spinner
  - Running healthy: normal colors, normal spinner
  - Finished failed: entire row dimmed red (#9B4444)
  - Finished ok: entire row dimmed gray — fades into background
  - Cursor highlight: all text brightens, full-width background highlight

  Sorting:
  - Default sort by status urgency:
    running+failing → running healthy → finished failed → finished ok
  - →← moves highlight across sortable column headers
  - s toggles sort on highlighted column (▼ desc → ▲ asc → ▼ desc)
  - Sortable columns: Status, ID, Command, Mode, Progress, Time, Age
  - Sort arrow visible on active column, highlight visible on navigated column
  - The list scrolls like htop — full list, viewport follows cursor

  Query bar:
  - Always visible as a line above the footer
  - Shows active query with ● indicator, or "/: query" hint when empty
  - Press / to edit — replaces query line with text input
  - enter applies, esc cancels
  - Query constrains which commands the agent sends (server-side filter)

  Keybindings:
  - ↑↓: select row (j/k also work, not advertised)
  - PgUp/PgDn: jump one screenful up/down
  - enter: open command details
  - →←: move sort column highlight
  - s: toggle sort on highlighted column
  - /: edit query
  - q: quit
  - ?: help
  - Footer hints drop dynamically when terminal is too narrow
    Descriptions truncate first, then hints drop entirely

  Responsive column priority (what drops as terminal narrows):
  - Always visible: Status, ID, Progress bar (min ~10 chars), ✓, ✗, Time
  - Medium drops: Mode, ◐, ○, Age
  - Narrow drops: Command — just Status + ID + bar + ✓ + ✗ + Time

  ASCII fallback (TERM=dumb):
  - Column headers: OK  FL  IP  PD instead of ✓  ✗  ◐  ○
  - Bar: [====XX--....] instead of ████▒▒░░⋅⋅⋅⋅
    = done, X failed, - in-progress, . pending
  - Status: OK / FL instead of ✓ / ✗


================================================================================
VIEW 2: Single command detail — summary mode (after pressing 'enter')
================================================================================

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  ← esc/backspace                                                   ↻ live   │
  ├──────────────────────────────────────────────────────────────────────────────┤
  │       ID            Command  Mode       Progress              ✓  ✗  ◐  ○  Time│
  │      cmd-abc123    apply    reconcile  █████████████░░⋅⋅⋅⋅  10  0  2  3  00:42│
  ├──────────────────────────────────────────────────────────────────────────────┤
  │                                                                              │
  │  Targets                                                                     │
  │       Label                                                Operation  Time   │
  │  ✓    aws-us-east-1                                        create    00:03   │
  │                                                                              │
  │  Stacks                                                                      │
  │       Label                                                Operation  Time   │
  │  ✓    production                                           create    00:01   │
  │                                                                              │
  │  Policies                                                                    │
  │       Label                     Type                       Operation  Time   │
  │  ✓    auto-reconcile            ttl, production            update    00:01   │
  │                                                                              │
  │  Resources                                                                   │
  │       Label                     Type                       Operation  Time   │
  │  ◐    my-bucket                 AWS::S3::Bucket            create    00:05   │
  │  ✓    web-1                     AWS::EC2::Instance         create    00:08   │
  │  ✓    web-2                     AWS::EC2::Instance         create    00:07   │
  │  ◐    primary                   AWS::RDS::DBInstance       update    00:12   │
  │  ✓    web-3                     AWS::EC2::Instance         create    00:06   │
  │  ✓    web-4                     AWS::EC2::Instance         create    00:05   │
  │  ✓    web-5                     AWS::EC2::Instance         create    00:07   │
  │  ✓    web-6                     AWS::EC2::Instance         create    00:06   │
  │  ✓    web-7                     AWS::EC2::Instance         create    00:08   │
  │  ✓    web-8                     AWS::EC2::Instance         create    00:05   │
  │  ○    cdn                       AWS::CloudFront::Dist…     create       —    │
  │                                                                              │
  ├──────────────────────────────────────────────────────────────────────────────┤
  │  space: expand  /: query  esc: back                               ?: help │
  └──────────────────────────────────────────────────────────────────────────────┘

  Notes:
  - Top: same htop-style command row as multi-command view (reused layout)
  - Below separator: resource table with same column philosophy
    Status symbol on far left, Label + Type first, then Operation, then Time
  - Updates grouped by type: Targets, Stacks, Policies, Resources
  - Failed/skipped rows show error message or dependency on next line
  - d: toggle to bordered detail cards (VIEW 3)
  - ← esc/backspace returns to multi-command list
  - Same responsive column dropping as multi-command view


================================================================================
VIEW 3: Single command — expanded detail mode (after pressing 'd')
================================================================================

  ┌─────────────────────────────────────────────────────────────────────────────┐
  │  ← esc/backspace                                                  ↻ live   │
  │                                                                             │
  │  apply reconcile  cmd-abc123                              elapsed: 00:42   │
  │  ██████████████████████████████████░░░░░⋅⋅⋅⋅⋅  12/15 updates              │
  ├─────────────────────────────────────────────────────────────────────────────┤
  │                                                                             │
  │  Targets                                                                    │
  │  ┌─ create aws-us-east-1 ─────────────────────────────────────── ✓ ──┐  │
  │  │  Discoverable: yes                                                    │  │
  │  │  Duration:     3s                                                     │  │
  │  └───────────────────────────────────────────────────────────────────────┘  │
  │                                                                             │
  │  Stacks                                                                     │
  │  ┌─ create production ────────────────────────────────────────── ✓ ──┐  │
  │  │  Description:  Production environment                                 │  │
  │  │  Duration:     1s                                                     │  │
  │  └───────────────────────────────────────────────────────────────────────┘  │
  │                                                                             │
  │  Policies                                                                   │
  │  ┌─ update auto-reconcile (ttl, production) ──────────────── ✓ ─────┐  │
  │  │  Type:     ttl                                                        │  │
  │  │  Stack:    production                                                 │  │
  │  │  Duration: 1s                                                         │  │
  │  └───────────────────────────────────────────────────────────────────────┘  │
  │                                                                             │
  │  Resources                                                                  │
  │  ┌─ create my-bucket (AWS::S3::Bucket) ────────────────── ◐ 00:05 ─────┐  │
  │  │  Type:     AWS::S3::Bucket                                            │  │
  │  │  Stack:    production                                                 │  │
  │  │  Started:  2m ago                                                     │  │
  │  │  Attempt:  1/9                                                        │  │
  │  │  Status:   Creating resource...                                       │  │
  │  └───────────────────────────────────────────────────────────────────────┘  │
  │                                                                             │
  │  ┌─ create web-1 (AWS::EC2::Instance) ─────────────────────── ✓ ────┐  │
  │  │  Type:     AWS::EC2::Instance                                         │  │
  │  │  Stack:    production                                                 │  │
  │  │  Duration: 8s                                                         │  │
  │  └───────────────────────────────────────────────────────────────────────┘  │
  │                                                                             │
  │  ┌─ update primary (AWS::RDS::DBInstance) ─────────────── ◐ 00:12 ─────┐  │
  │  │  Type:     AWS::RDS::DBInstance                                       │  │
  │  │  Stack:    production                                                 │  │
  │  │  Started:  12s ago                                                    │  │
  │  │  Attempt:  2/9                                                        │  │
  │  │  Status:   Modifying database instance...                             │  │
  │  └───────────────────────────────────────────────────────────────────────┘  │
  │                                                                             │
  ├─────────────────────────────────────────────────────────────────────────────┤
  │  space: expand  /: query  esc: back                                ?: help  │
  └─────────────────────────────────────────────────────────────────────────────┘

  Notes:
  - Detail mode wraps each update in a bordered card
  - Resource cards show: Type, Stack, Duration/Started, Attempt (X/MaxAttempts),
    Status message (in-progress) or Error message (failed)
  - Target cards show: Discoverable, Duration, ErrorMessage if failed
  - Stack cards show: Description, Duration, ErrorMessage if failed
  - Policy cards show: Type (ttl/auto-reconcile), Stack, Duration,
    PolicyConfig changes (for updates), ReferencingStacks (for skips)
  - This view scrolls — only a few cards visible at once


================================================================================
VIEW 3b: Detail mode — failed resource card
================================================================================

  │  ┌─ delete old-data (AWS::S3::Bucket) ──────────────────────── ✗ ──────┐  │
  │  │  Type:     AWS::S3::Bucket                                            │  │
  │  │  Stack:    production                                                 │  │
  │  │  Duration: 4s                                                         │  │
  │  │  Attempt:  9/9                                                        │  │
  │  │  Error:    BucketNotEmpty: The bucket you tried to delete is not      │  │
  │  │            empty                                                      │  │
  │  └───────────────────────────────────────────────────────────────────────┘  │

  Notes:
  - Failed card border in red
  - Error message shown in red
  - Attempt shows max reached (9/9)


================================================================================
VIEW 3c: Detail mode — cascade / skipped resource card
================================================================================

  │  ┌─ delete legacy-3 (AWS::EC2::Instance) ────────────────── ⊘ ────────┐  │
  │  │  Type:       AWS::EC2::Instance                                       │  │
  │  │  Stack:      production                                               │  │
  │  │  Cascade:    yes (depends on legacy-2)                                │  │
  │  └───────────────────────────────────────────────────────────────────────┘  │

  Notes:
  - Skipped card shows cascade source
  - IsCascade + CascadeSource from ResourceUpdate


================================================================================
VIEW 3d: Detail mode — policy skip with referencing stacks
================================================================================

  │  ┌─ delete retention-policy (ttl, staging) ────────────── Skipped ─────┐  │
  │  │  Type:               ttl                                              │  │
  │  │  Stack:              staging                                          │  │
  │  │  Referencing stacks: production, development                          │  │
  │  └───────────────────────────────────────────────────────────────────────┘  │

  Notes:
  - PolicyUpdate.ReferencingStacks shown when policy is skipped
    because other stacks still reference it


================================================================================
SUPERSEDED VIEWS (replaced by new design)
================================================================================

  The following original mockup views have been replaced by the new design:

  - VIEW 4 (auto-collapsed groups): replaced by per-group tables with
    pagination ("show 10 more"). Groups are always visible as table sections,
    not collapsible tree nodes. Pagination shows first 10 rows per group,
    expand with enter/space on the "show more" row.

  - VIEW 5 (filter by state) and VIEW 6 (search): replaced by the query bar.
    The query bar uses formae query language (same as --query flag) and
    filters server-side. No separate filter/search modes needed.

  - VIEW 7 (auto-follow): not needed. In a live-updating view with sort,
    new commands sort into position automatically. The cursor stays where
    the user put it. No auto-follow toggle required.

  - VIEW 8 (command complete state) and VIEW 9 (failed final state):
    replaced by row coloring and sort behavior. Completed commands sort to
    the bottom (dimmed). Failed commands show in red (dimmed red if finished,
    bright red if still running). No special "complete" view needed — the
    main view handles all states through coloring and sort order.


================================================================================
DATA FIELDS COVERAGE
================================================================================

Command fields shown in main view:
  ID, CmdType (Command), Mode, State (as symbol), Progress (bar or text),
  Per-status counts (✓ ✗ ◐ ○), Duration (Time), StartedAt (Age)

ResourceUpdate fields shown in subview:
  Table mode: State (symbol), Label, Type, Operation, Duration (Time)
              ErrorMessage on second line for failed, CascadeSource for skipped
  Detail card: + Stack, CurrentAttempt/MaxAttempts, StatusMessage

TargetUpdate fields shown in subview:
  Table mode: State (symbol), Label, Operation, Duration
  Detail card: + Discoverable, ErrorMessage

StackUpdate fields shown in subview:
  Table mode: State (symbol), Label, Operation, Duration
  Detail card: + Description, ErrorMessage

PolicyUpdate fields shown in subview:
  Table mode: State (symbol), Label, Type, Stack, Operation, Duration
  Detail card: + PolicyConfig/OldPolicyConfig (for updates),
               ReferencingStacks (for skips), ErrorMessage

Fields NOT shown (internal/derived):
  - ResourceID (internal ksuid, not user-facing)
  - OldStackName (shown only for stack migration, handled separately)
  - Properties/OldProperties (used for patch rendering in simulation)
  - ReferenceLabels (used for reference resolution display)
  - GroupID (internal grouping)


================================================================================
COLOR KEY
================================================================================

  Element                        Formae Theme         Classic Theme
  ─────────────────────────────────────────────────────────────────
  Panel borders                  #333355 (dark blue)  #444444 (gray)
  Command ID / Label (active)    #60A5FA (blue)       #ADD8E6 (light blue)
  Command ID / Label (cursor)    #93C5FD (bright blue)
  Progress bar █ done            #4ADE80 (green)      #00FF00 (green)
  Progress bar ▒ failed          #F87171 (red)        #FF6666 (red)
  Progress bar ░ in-progress     #AAAAAA (dim)        #ADD8E6 (light blue)
  Progress bar ⋅ pending         #555555 (dark gray)  #808080 (gray)

  Row: running healthy           normal colors, spinner in InProgress color
  Row: running + failing         all text #F87171 (bright red), red spinner
  Row: finished failed           all text #9B4444 (dimmed red)
  Row: finished ok               all text TextSubtle (dimmed gray)
  Row: cursor highlight          bg #4E4E4E (239), all text brightened
  Row: cursor on finished ok     all text TextSecondary
  Row: cursor on failed          all text #FCA5A5 (bright red)

  Section headers (▌ Resources)  SecondaryAccent (orange), bold
  Detail card border             Border (default), PrimaryAccent (selected)
  Detail card border (failed)    Error (red)
  Keybinding keys                #60A5FA (blue)       #ADD8E6 (light blue)
  Keybinding descriptions        #555555 (subtle)     #555555 (gray)
  Query indicator ●              SecondaryAccent (orange)


================================================================================
KEYBINDINGS (resolved from RFC PR review)
================================================================================

  Keybindings follow de facto TUI standards (htop, lazygit, k9s):

  Main view:
    ↑↓          select row (j/k silently supported)
    PgUp/PgDn   jump one screenful
    enter        open command details
    →←           move sort column highlight
    s            toggle sort on highlighted column (▼/▲)
    /            edit query (in query bar above footer)
    q            quit
    ?            help

  Single command subview:
    ↑↓          select row (j/k silently supported)
    PgUp/PgDn   jump one screenful
    enter/space  expand/collapse resource detail card
    →←           move sort column highlight (per-group)
    s            toggle sort on highlighted column (per-group)
    /            edit query
    esc/backspace  back to main view

  Design decisions:
  - Only universally known keys are advertised in footer
  - vim keys (j/k/h/l) work but aren't shown
  - Footer hints drop dynamically on narrow terminals
  - Descriptions truncate before hints disappear entirely
  - ? help overlay: TODO — not yet designed
